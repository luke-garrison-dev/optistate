<?php if (!defined("ABSPATH")) { exit(); } class OPTISTATE_DB_Wrapper { private static ?OPTISTATE_DB_Wrapper $instance = null; private ?mysqli $connection = null; private int $max_retries = 3; private ?array $db_config_cache = null; private ?int $connection_created_at = null; private int $query_count = 0; private int $total_queries_executed = 0; private int $connection_errors = 0; private const MAX_QUERY_COUNT = 10000; private const MAX_CONNECTION_AGE = 300; private bool $transaction_active = false; private ?int $transaction_start_time = null; private array $init_commands = []; private array $active_locks = []; private array $persistent_locks = []; private ?int $last_ping = null; private ?string $cached_wp_timezone = null; private bool $timezone_cache_populated = false; private function __construct() { register_shutdown_function([$this, "emergency_cleanup"]); } public function set_session_state(string $query): bool { $result = $this->query($query); if ($result !== false) { $this->init_commands[] = $query; } return $result; } public static function get_instance(): self { if (self::$instance === null) { self::$instance = new self(); } if (self::$instance->should_refresh_connection()) { self::$instance->refresh_connection(); } return self::$instance; } public static function close_if_instantiated(): void { if (self::$instance !== null) { self::$instance->close(); } } private function should_refresh_connection(): bool { if ($this->connection === null) { return true; } if ($this->transaction_active || !empty($this->active_locks)) { return false; } if ($this->query_count >= self::MAX_QUERY_COUNT) { return true; } if ($this->connection_created_at && time() - $this->connection_created_at >= self::MAX_CONNECTION_AGE) { return true; } if ($this->last_ping === null || time() - $this->last_ping > 30) { if (!$this->is_connection_alive()) { return true; } $this->last_ping = time(); } return false; } private function is_connection_alive(): bool { if ($this->connection === null) { return false; } try { $result = @$this->connection->query("SELECT 1"); if ($result !== false) { if ($result instanceof mysqli_result) { $result->free(); } return true; } return false; } catch (Exception $e) { return false; } } private function refresh_connection(): void { $to_replay = $this->persistent_locks; $this->close(); $this->get_connection(); if ($this->connection) { if (!empty($this->init_commands)) { foreach ($this->init_commands as $cmd) { @$this->connection->query($cmd); } } if (!empty($to_replay)) { foreach ($to_replay as $lock_name => $timeout) { $escaped = $this->connection->real_escape_string($lock_name); $res = @$this->connection->query("SELECT GET_LOCK('$escaped', " . (int) $timeout . ")"); $reacquired = false; if ($res instanceof mysqli_result) { $row = $res->fetch_row(); $reacquired = $row && (int) $row[0] === 1; $res->free(); } if ($reacquired) { $this->persistent_locks[$lock_name] = (int) $timeout; $this->active_locks[$lock_name] = ["timeout" => (int) $timeout, "acquired_at" => time(), "lock_result" => 1, "query" => "REPLAY GET_LOCK",]; } else { unset($this->persistent_locks[$lock_name]); OPTISTATE_Utils::log_critical_error("Advisory lock could not be re-acquired after reconnect", ["lock_name" => $lock_name]); } } } } } public function get_connection(): mysqli { if ($this->connection !== null) { return $this->connection; } mysqli_report(MYSQLI_REPORT_OFF); global $wpdb; if ((class_exists("HyperDB") && is_a($wpdb, "HyperDB")) || (class_exists("LudicrousDB") && is_a($wpdb, "LudicrousDB")) || (isset($wpdb->is_mysql) && !$wpdb->is_mysql)) { $connection_start = microtime(true); if (method_exists($wpdb, "send_reads_to_masters")) { $wpdb->send_reads_to_masters(); } $wpdb->db_connect(); if (isset($wpdb->dbh) && $wpdb->dbh instanceof mysqli) { $this->connection = $wpdb->dbh; $this->connection_created_at = time(); $this->query_count = 0; $this->last_ping = time(); if (!empty($this->init_commands)) { foreach ($this->init_commands as $cmd) { @$this->connection->query($cmd); } } $this->apply_default_session_settings(); return $this->connection; } } if ($this->db_config_cache === null) { $db_host = !empty($wpdb->dbhost) ? $wpdb->dbhost : DB_HOST; $db_user = !empty($wpdb->dbuser) ? $wpdb->dbuser : DB_USER; $db_pass = !empty($wpdb->dbpassword) ? $wpdb->dbpassword : DB_PASSWORD; $db_name = !empty($wpdb->dbname) ? $wpdb->dbname : DB_NAME; $port = null; $socket = null; if (preg_match('/^[^:]+:(\/.*)$/', $db_host, $matches)) { $socket = $matches[1]; $db_host = substr($db_host, 0, strpos($db_host, ":")); } elseif (strpos($db_host, ":") !== false) { $parts = explode(":", $db_host); $db_host = $parts[0]; $port = (int) $parts[1]; } $this->db_config_cache = ["host" => $db_host, "user" => $db_user, "pass" => $db_pass, "name" => $db_name, "port" => $port, "socket" => $socket,]; } $db_host = $this->db_config_cache["host"]; $port = $this->db_config_cache["port"]; $socket = $this->db_config_cache["socket"]; $client_flags = defined("MYSQL_CLIENT_FLAGS") ? MYSQL_CLIENT_FLAGS : 0; $attempts = 0; $last_error = ""; $connection_start = microtime(true); while ($attempts < $this->max_retries) { try { $mysqli = mysqli_init(); if (!$mysqli) { throw new Exception("mysqli_init failed"); } $timeout = defined("DOING_CRON") && DOING_CRON ? 20 : 5; $mysqli->options(MYSQLI_OPT_CONNECT_TIMEOUT, $timeout); if (defined("MYSQLI_OPT_READ_TIMEOUT")) { $mysqli->options(MYSQLI_OPT_READ_TIMEOUT, $timeout * 2); } if (defined("MYSQLI_OPT_WRITE_TIMEOUT")) { $mysqli->options(MYSQLI_OPT_WRITE_TIMEOUT, $timeout * 2); } if (defined("MYSQLI_OPT_INT_AND_FLOAT_NATIVE")) { $mysqli->options(MYSQLI_OPT_INT_AND_FLOAT_NATIVE, true); } if (defined("MYSQL_SSL_CA") && MYSQL_SSL_CA) { $mysqli->ssl_set(defined("MYSQL_SSL_KEY") ? MYSQL_SSL_KEY : null, defined("MYSQL_SSL_CERT") ? MYSQL_SSL_CERT : null, MYSQL_SSL_CA, defined("MYSQL_SSL_CAPATH") ? MYSQL_SSL_CAPATH : null, defined("MYSQL_SSL_CIPHER") ? MYSQL_SSL_CIPHER : null); } try { $connected = $mysqli->real_connect($db_host, $this->db_config_cache["user"], $this->db_config_cache["pass"], $this->db_config_cache["name"], $port, $socket, $client_flags); } catch (\Throwable $e) { throw new Exception($e->getMessage()); } if ($connected) { $this->apply_default_session_settings($mysqli); $this->connection = $mysqli; $this->connection_created_at = time(); $this->query_count = 0; $this->last_ping = time(); return $this->connection; } else { $error_msg = $mysqli->connect_error . " (" . $mysqli->connect_errno . ")"; @$mysqli->close(); throw new Exception($error_msg); } } catch (Exception $e) { $last_error = $e->getMessage(); $attempts++; $this->connection_errors++; if ($attempts < $this->max_retries) { $is_web_request = !defined("WP_CLI") && !wp_doing_cron(); if ($is_web_request) { $base_delay = 200000 * $attempts; $jitter = wp_rand(0, 100000); } else { $base_delay = (int) (pow(2, $attempts - 1) * 1000000); $jitter = wp_rand(0, 1000000); } usleep($base_delay + $jitter); } } } OPTISTATE_Utils::log_critical_error("Failed to connect to database after " . $this->max_retries . " attempts", ["host" => $db_host, "user" => $this->db_config_cache["user"], "db" => $this->db_config_cache["name"], "last_error" => $last_error, "connection_errors" => $this->connection_errors,]); throw new Exception(sprintf(__("Failed to connect to database after %s attempts. Error: %s", "optistate"), number_format_i18n($this->max_retries), $last_error)); } private function apply_default_session_settings(?\mysqli $connection = null): void { $connection = $connection ?? $this->connection; if (!$connection) { return; } global $wpdb; $charset = !empty($wpdb->charset) ? $wpdb->charset : (defined("DB_CHARSET") ? DB_CHARSET : "utf8mb4"); $collate = !empty($wpdb->collate) ? $wpdb->collate : (defined("DB_COLLATE") ? DB_COLLATE : ""); $connection->set_charset($charset); if ($collate !== "") { $connection->query("SET collation_connection = '$collate'"); } $wp_timezone = $this->get_wp_timezone(); if ($wp_timezone) { $connection->query("SET SESSION time_zone = '" . $connection->real_escape_string($wp_timezone) . "'"); } $connection->query("SET SESSION sql_mode='NO_ENGINE_SUBSTITUTION'"); $connection->query("SET SESSION wait_timeout=300"); $connection->query("SET SESSION interactive_timeout=300"); } private function get_wp_timezone(): ?string { if ($this->timezone_cache_populated) { return $this->cached_wp_timezone; } global $wpdb; $wp_timezone = $wpdb->get_var("SELECT @@session.time_zone"); $this->cached_wp_timezone = $wp_timezone !== null && $wp_timezone !== "" ? $wp_timezone : null; $this->timezone_cache_populated = true; return $this->cached_wp_timezone; } public function force_commit_if_needed(): bool { if (!$this->transaction_active || $this->query_count < self::MAX_QUERY_COUNT - 100) { return true; } $committed = $this->commit(); if (!$committed) { $this->rollback(); return false; } $this->refresh_connection(); $newTx = $this->begin_transaction(); if ($newTx === false) { OPTISTATE_Utils::log_critical_error("force_commit_if_needed: failed to restart transaction"); return false; } return true; } public function query(string $query) { $connection = $this->get_connection(); $is_lock_query = (bool) preg_match("/\bGET_LOCK\b|\bRELEASE_LOCK\b/i", $query); if ($is_lock_query) { $this->track_lock_operation($query); } $this->query_count++; $this->total_queries_executed++; $result = $connection->query($query); if ($result === false) { $errno = $connection->errno; if (in_array($errno, [2006, 2013], true) && empty($this->active_locks)) { if ($this->transaction_active) { $this->transaction_active = false; $this->transaction_start_time = null; throw new Exception("MySQL server connection dropped during an active transaction. Batch consistency lost; safe restart required."); } $this->query_count--; $this->total_queries_executed--; @$connection->close(); $this->connection = null; $this->last_ping = null; $connection = $this->get_connection(); if (!empty($this->init_commands)) { foreach ($this->init_commands as $cmd) { @$connection->query($cmd); } } $this->query_count++; $this->total_queries_executed++; $result = $connection->query($query); if ($result === false) { OPTISTATE_Utils::log_critical_error("Query failed after connection retry", ["errno" => $errno, "query_preview" => substr($query, 0, 200), "error" => $connection->error, "in_transaction" => $this->transaction_active, "active_locks" => array_keys($this->active_locks), "connection_id" => $connection->thread_id ?? null,]); } } else { OPTISTATE_Utils::log_critical_error("Query execution failed", ["errno" => $errno, "query_preview" => substr($query, 0, 200), "error" => $connection->error, "in_transaction" => $this->transaction_active, "active_locks" => array_keys($this->active_locks), "connection_id" => $connection->thread_id ?? null,]); } } if ($is_lock_query) { $this->update_lock_status($query, $result); } return $result; } public function is_lock_held(string $lock_name): bool { return isset($this->active_locks[$lock_name]) && ($this->active_locks[$lock_name]["lock_result"] ?? null) === 1; } public function acquire_persistent_lock(string $lock_name, int $timeout = 5): bool { $connection = $this->get_connection(); $escaped = $connection->real_escape_string($lock_name); $result = $this->query("SELECT GET_LOCK('$escaped', " . (int) $timeout . ")"); if (!$this->is_lock_held($lock_name)) { return false; } $this->persistent_locks[$lock_name] = (int) $timeout; return true; } public function release_persistent_lock(string $lock_name): bool { unset($this->persistent_locks[$lock_name]); $connection = $this->get_connection(); $escaped = $connection->real_escape_string($lock_name); $result = $this->query("SELECT RELEASE_LOCK('$escaped')"); if ($result instanceof mysqli_result) { $row = $result->fetch_row(); $result->free(); return $row && (int) $row[0] === 1; } return false; } public function get_persistent_locks(): array { return array_keys($this->persistent_locks); } private function track_lock_operation(string $query): void { if (stripos($query, "LOCK") === false) { return; } if (preg_match("/SELECT\s+GET_LOCK\s*\(\s*['\"]([^'\"]+)['\"]\s*,\s*(\d+)\s*\)/i", $query, $matches)) { $lock_name = $matches[1]; $timeout = (int) $matches[2]; $this->active_locks[$lock_name] = ["timeout" => $timeout, "acquired_at" => time(), "lock_result" => null, "query" => $query,]; } elseif (preg_match("/SELECT\s+RELEASE_LOCK\s*\(\s*['\"]([^'\"]+)['\"]\s*\)/i", $query, $matches)) { $lock_name = $matches[1]; if (isset($this->active_locks[$lock_name])) { $this->active_locks[$lock_name]["pending_release"] = true; } } } private function update_lock_status(string $query, $result): void { if (stripos($query, "LOCK") === false) { return; } if (preg_match("/SELECT\s+GET_LOCK\s*\(\s*['\"]([^'\"]+)['\"]\s*,/i", $query, $matches)) { $lock_name = $matches[1]; if ($result instanceof mysqli_result) { $row = $result->fetch_row(); $lock_result = $row ? (int) $row[0] : 0; $result->free(); if ($lock_result === 1) { if (isset($this->active_locks[$lock_name])) { $this->active_locks[$lock_name]["lock_result"] = 1; } } else { unset($this->active_locks[$lock_name]); } } else { unset($this->active_locks[$lock_name]); } } elseif (preg_match("/SELECT\s+RELEASE_LOCK\s*\(\s*['\"]([^'\"]+)['\"]\s*\)/i", $query, $matches)) { $lock_name = $matches[1]; if ($result instanceof mysqli_result) { $row = $result->fetch_row(); $release_result = $row !== null && $row !== false ? (int) $row[0] : 0; $result->free(); if ($release_result === 1) { unset($this->active_locks[$lock_name]); } else { OPTISTATE_Utils::log_critical_error("Failed to release MySQL lock", ["lock_name" => $lock_name, "result" => $release_result]); } } else { unset($this->active_locks[$lock_name]); OPTISTATE_Utils::log_critical_error("RELEASE_LOCK query failed outright", ["lock_name" => $lock_name]); } } } public function begin_transaction() { if ($this->transaction_active) { return true; } $connection = $this->get_connection(); $connection->autocommit(false); $result = $connection->begin_transaction(); if ($result) { $this->transaction_active = true; $this->transaction_start_time = time(); return true; } OPTISTATE_Utils::log_critical_error("Failed to begin transaction", ["error" => $connection->error,]); return false; } public function commit(): bool { if (!$this->transaction_active) { return false; } $connection = $this->get_connection(); $result = $connection->commit(); if ($result) { $this->transaction_active = false; $this->transaction_start_time = null; $connection->autocommit(true); return true; } OPTISTATE_Utils::log_critical_error("Failed to commit transaction", ["error" => $connection->error, "queries_in_transaction" => $this->query_count,]); return false; } public function rollback($savepoint = null): bool { if (!$this->transaction_active) { return false; } $connection = $this->get_connection(); if ($savepoint !== null) { $result = $this->query("ROLLBACK TO SAVEPOINT {$savepoint}"); if ($result !== false) { return true; } OPTISTATE_Utils::log_critical_error("Failed to rollback to savepoint", ["savepoint" => $savepoint]); return false; } $result = $connection->rollback(); if ($result) { $this->transaction_active = false; $this->transaction_start_time = null; $connection->autocommit(true); return true; } OPTISTATE_Utils::log_critical_error("Failed to rollback transaction", ["error" => $connection->error,]); return false; } public function in_transaction(): bool { return $this->transaction_active; } public function get_error(): string { return $this->connection ? $this->connection->error : __("No active database connection.", "optistate"); } public function get_errno(): int { return $this->connection ? $this->connection->errno : 0; } public function close(): void { if ($this->connection !== null) { if (!empty($this->active_locks)) { foreach (array_keys($this->active_locks) as $lock_name) { $release_query = sprintf("SELECT RELEASE_LOCK('%s')", $this->connection->real_escape_string($lock_name)); $result = @$this->connection->query($release_query); if ($result === false) { OPTISTATE_Utils::log_critical_error("Failed to release lock on close", ["lock_name" => $lock_name]); } } $this->active_locks = []; } if ($this->transaction_active) { @$this->connection->rollback(); @$this->connection->autocommit(true); $this->transaction_active = false; $this->transaction_start_time = null; } @$this->connection->close(); $this->connection = null; $this->connection_created_at = null; $this->query_count = 0; $this->last_ping = null; } } public function emergency_cleanup(): void { if ($this->transaction_active || !empty($this->active_locks)) { try { if ($this->connection !== null) { $is_alive = $this->is_connection_alive(); if ($is_alive) { if ($this->transaction_active) { @$this->connection->rollback(); @$this->connection->autocommit(true); } if (!empty($this->active_locks)) { foreach (array_keys($this->active_locks) as $lock_name) { @$this->connection->query(sprintf("SELECT RELEASE_LOCK('%s')", $this->connection->real_escape_string($lock_name))); } $this->active_locks = []; } } else { $this->active_locks = []; @$this->connection->close(); $this->connection = null; } } } catch (\Throwable $e) { OPTISTATE_Utils::log_critical_error("Emergency cleanup exception", ["message" => $e->getMessage()]); $this->active_locks = []; if ($this->connection !== null) { $this->connection = null; } } finally { $this->transaction_active = false; $this->transaction_start_time = null; } } if ($this->connection !== null && $this->is_connection_alive()) { try { @$this->connection->query("SET FOREIGN_KEY_CHECKS = 1"); @$this->connection->query("SET AUTOCOMMIT = 1"); } catch (\Throwable $e) { } } $this->close(); if (function_exists("gc_collect_cycles")) { gc_collect_cycles(); } } }