<?php declare(strict_types=1); if (!defined("ABSPATH")) { exit(); } class OPTISTATE_Settings_Manager { private OPTISTATE $main_plugin; private ?array $settings_cache = null; private static ?bool $table_exists_cache = null; const SETTINGS_CACHED_OPTION = "optistate_settings_cached"; public function __construct(OPTISTATE $main_plugin) { $this->main_plugin = $main_plugin; } private function get_settings_version(): int { return (int) get_option("optistate_settings_version", 1); } private function increment_settings_version(): void { $new_version = $this->get_settings_version() + 1; update_option("optistate_settings_version", $new_version, true); wp_cache_delete("optistate_settings_rows_v" . ($new_version - 1), "optistate"); } public function get_persistent_settings(): array { if ($this->settings_cache !== null) { return $this->settings_cache; } $version = $this->get_settings_version(); $cache_key = "optistate_settings_rows_v" . $version; $cached = wp_cache_get($cache_key, "optistate"); if (is_array($cached)) { $this->settings_cache = $cached; return $cached; } $option_settings = get_option(self::SETTINGS_CACHED_OPTION); if (is_array($option_settings)) { $defaults = $this->get_default_settings(); $settings = array_merge($defaults, $option_settings); $validated = $this->validate_settings($settings); if (isset($validated["pagespeed_api_key"])) { $validated["pagespeed_api_key"] = OPTISTATE_Utils::decrypt_data($validated["pagespeed_api_key"]); } $this->settings_cache = $validated; wp_cache_set($cache_key, $validated, "optistate", HOUR_IN_SECONDS); return $validated; } $this->ensure_table_exists(); $stored_raw = $this->main_plugin->get_store_data("settings", []); if (!is_array($stored_raw)) { $stored_raw = []; } $defaults = $this->get_default_settings(); $settings = array_merge($defaults, $stored_raw); $validated = $this->validate_settings($settings); if ($settings !== $validated || empty($stored_raw)) { $this->main_plugin->set_store_data("settings", $validated); } update_option(self::SETTINGS_CACHED_OPTION, $validated, true); $return_data = $validated; if (isset($return_data["pagespeed_api_key"])) { $return_data["pagespeed_api_key"] = OPTISTATE_Utils::decrypt_data($return_data["pagespeed_api_key"]); } $this->settings_cache = $return_data; wp_cache_set($cache_key, $return_data, "optistate", HOUR_IN_SECONDS); return $return_data; } public function save_persistent_settings(array $new_settings): bool { try { $this->ensure_table_exists(); $current = $this->get_persistent_settings(); $merged = array_merge($current, $new_settings); $validated = $this->validate_settings($merged); $old_version = $this->get_settings_version(); $result = $this->main_plugin->set_store_data("settings", $validated); if (!$result) { return false; } $option_updated = update_option(self::SETTINGS_CACHED_OPTION, $validated, true); if ($option_updated === false && get_option(self::SETTINGS_CACHED_OPTION) !== $validated) { OPTISTATE_Utils::log_critical_error("SETTINGS_CACHED_OPTION update returned false and value differs", ["validated_hash" => md5(serialize($validated))]); } $this->increment_settings_version(); wp_cache_delete("optistate_settings_rows_v" . $old_version, "optistate"); $this->settings_cache = null; $this->main_plugin->clear_directory_existence_cache(); OPTISTATE_Utils::clear_table_existence_cache(); $this->main_plugin->invalidate_plugin_caches(); self::reset_table_cache(); return true; } catch (\Throwable $e) { OPTISTATE_Utils::log_critical_error("save_persistent_settings failed: " . $e->getMessage(), ["trace" => $e->getTraceAsString()]); return false; } } public function get_default_settings(): array { static $defaults = null; if ($defaults !== null) { return $defaults; } $defaults = ["max_backups" => 3, "auto_optimize_days" => 0, "auto_optimize_time" => "02:00", "email_notifications" => false, "auto_backup_only" => false, "performance_features" => [], "disable_restore_security" => false, "allowed_users" => [], "pagespeed_api_key" => OPTISTATE_Utils::encrypt_data("AIzaSyDGo4ufkdrd2e8za0IRz_qnX-GWQdaBA2s"), "login_protect_enabled" => false, "login_protect_max_attempts" => 3, "login_protect_block_duration" => 6, "cloudflare_enabled" => false, "custom_trusted_proxies" => [], "login_captcha_enabled" => false, "ip_blocker_enabled" => false, "ip_block_list" => [], "ip_whitelist" => [], "enable_two_factor" => false, "one_click_extra_items" => [], "one_click_backup" => false, "last_applied_preset" => "",]; return $defaults; } private function validate_settings(array $settings): array { return $this->_validate_settings_array($settings); } private function _validate_settings_array(array $settings): array { static $all_cleanup_items = null; if ($all_cleanup_items === null) { $all_cleanup_items = OPTISTATE_Cleanup_Functions::get_all_cleanup_items(); } $defaults = $this->get_default_settings(); $validated = []; foreach ($defaults as $key => $default_value) { $value = $settings[$key] ?? $default_value; switch ($key) { case "max_backups": $validated[$key] = max(1, min(10, absint($value))); break; case "auto_optimize_days": $validated[$key] = max(0, min(365, absint($value))); break; case "auto_optimize_time": $validated[$key] = preg_match('/^([01]?[0-9]|2[0-3]):[0-5][0-9]$/', (string) $value) ? (string) $value : "02:00"; break; case "one_click_backup": $validated[$key] = (bool) $value; break; case "last_applied_preset": $validated[$key] = is_string($value) ? sanitize_key($value) : ""; break; case "email_notifications": case "auto_backup_only": case "disable_restore_security": case "login_protect_enabled": case "cloudflare_enabled": case "ip_blocker_enabled": $validated[$key] = (bool) $value; break; case "login_captcha_enabled": $validated[$key] = (bool) $value; break; case "performance_features": $validated[$key] = is_array($value) ? $value : []; break; case "allowed_users": $validated[$key] = is_array($value) ? array_values(array_unique(array_filter(array_map("absint", $value), function ($id) { return $id > 0; }))) : []; break; case "pagespeed_api_key": $key_val = trim((string) $value); if (empty($key_val)) { $key_val = $this->get_default_settings()["pagespeed_api_key"]; } else { if (strpos($key_val, "enc:") !== 0) { $key_val = OPTISTATE_Utils::encrypt_data($key_val); } } $validated[$key] = $key_val; break; case "enable_two_factor": $validated[$key] = (bool) $value; break; case "login_protect_max_attempts": $validated[$key] = in_array(absint($value), [1, 2, 3, 4, 5, 6], true) ? absint($value) : 3; break; case "login_protect_block_duration": $validated[$key] = in_array(absint($value), [1, 3, 6, 12, 24, 48], true) ? absint($value) : 6; break; case "custom_trusted_proxies": if (is_array($value)) { $validated[$key] = array_values(array_unique(array_filter($value, function ($ip) { return OPTISTATE_Utils::validate_ip_or_cidr((string) $ip); }))); } else { $validated[$key] = []; } break; case "ip_block_list": case "ip_whitelist": if (is_array($value)) { $filtered = array_values(array_unique(array_filter($value, function ($entry) { return OPTISTATE_Utils::validate_ip_or_cidr((string) $entry); }))); if (count($filtered) > 200) { $filtered = array_slice($filtered, 0, 200); } $validated[$key] = $filtered; } else { $validated[$key] = []; } break; case "one_click_extra_items": if (is_array($value)) { $validated[$key] = array_values(array_unique(array_filter($value, function ($k) use ($all_cleanup_items) { return isset($all_cleanup_items[$k]); }))); } else { $validated[$key] = []; } break; default: $validated[$key] = $value; } } return $validated; } public static function reset_table_cache(): void { self::$table_exists_cache = null; } private function ensure_table_exists(): void { if (self::$table_exists_cache === true) { return; } global $wpdb; $table_name = $wpdb->prefix . "optistate_core_data"; $sql = "data_key varchar(100) NOT NULL, data_value longtext NOT NULL, updated_at datetime NOT NULL, PRIMARY KEY (data_key), KEY updated_at (updated_at), KEY log_lookup (data_key, updated_at)"; $success = OPTISTATE_Utils::create_table_if_not_exists($table_name, $sql, true); if ($success) { self::$table_exists_cache = true; } } public function check_user_access(): bool { $error_message = __("You do not have sufficient permissions to perform this action.", "optistate"); if (!current_user_can("manage_options")) { if (defined("DOING_AJAX") && DOING_AJAX) { OPTISTATE_Utils::send_json_error($error_message, 403); } else { wp_die($error_message, 403); } return false; } $settings = $this->get_persistent_settings(); $allowed_users = $settings["allowed_users"] ?? []; if (empty($allowed_users)) { return true; } $current_user_id = get_current_user_id(); if (!in_array($current_user_id, $allowed_users, true)) { $restricted_msg = __("<strong>Access Restricted: Your administrator account is not on the allowed list for this plugin.</strong><br>✉︎ Please contact the system administrator to request access.", "optistate"); if (defined("DOING_AJAX") && DOING_AJAX) { OPTISTATE_Utils::send_json_error($restricted_msg, 403); } else { wp_die($restricted_msg, 403); } return false; } return true; } public function cleanup_deleted_user_from_access_list(int $user_id): void { $settings = $this->get_persistent_settings(); $allowed_users = $settings["allowed_users"] ?? []; if (empty($allowed_users)) { return; } $key = array_search($user_id, $allowed_users, true); if ($key !== false) { unset($allowed_users[$key]); $allowed_users = array_values($allowed_users); $this->save_persistent_settings(["allowed_users" => $allowed_users]); $this->main_plugin->log_entry(sprintf("🗑️ Removed deleted user (ID: %s) from access list", number_format_i18n($user_id)), "scheduled"); } } public function handle_settings_download(): void { if (!isset($_GET["action"]) || $_GET["action"] !== "optistate_download_settings") { return; } if (!is_user_logged_in()) { wp_die(esc_html__("Authentication required.", "optistate"), 403); } if (!isset($_GET["_wpnonce"]) || !wp_verify_nonce(sanitize_text_field(wp_unslash($_GET["_wpnonce"])), "optistate_download_settings")) { wp_die(esc_html__("Security check failed.", "optistate"), esc_html__("Security Error", "optistate"), ["response" => 403]); } if (!current_user_can("manage_options")) { wp_die(esc_html__("Insufficient permissions.", "optistate"), 403); } if (!$this->check_user_access()) { return; } try { $settings = $this->get_persistent_settings(); if (empty($settings)) { wp_die(esc_html__("No settings found to export.", "optistate"), esc_html__("Export Error", "optistate"), ["response" => 500]); } $cron_state = $this->main_plugin->get_store_data("cron_manager_state", []); $export_data = ["plugin" => "WP Optimal State", "version" => OPTISTATE::VERSION, "exported_at" => current_time("Y-m-d H:i:s"), "exported_timestamp" => time(), "site_url" => get_site_url(), "wp_version" => get_bloginfo("version"), "settings" => $settings, "cron_manager_state" => $cron_state,]; $json_content = wp_json_encode($export_data, JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE); if ($json_content === false) { wp_die(esc_html__("Failed to generate settings file.", "optistate"), esc_html__("Export Error", "optistate"), ["response" => 500]); } $this->main_plugin->log_entry("📥 " . __("Settings Exported by {username}", "optistate")); @ini_set('zlib.output_compression', 'Off'); @header_remove('Content-Encoding'); while (ob_get_level() > 0) { ob_end_clean(); } $filename = "optistate-settings-" . gmdate("Y-m-d-His") . ".json"; header("Content-Type: application/json; charset=utf-8"); header('Content-Disposition: attachment; filename="' . $filename . '"'); header("Content-Length: " . strlen($json_content)); header("Cache-Control: no-cache, no-store, must-revalidate"); header("Pragma: no-cache"); header("Expires: 0"); header("X-Content-Type-Options: nosniff"); header("X-Frame-Options: DENY"); echo $json_content; flush(); exit(); } catch (\Throwable $e) { OPTISTATE_Utils::log_critical_error("handle_settings_download failed: " . $e->getMessage(), ["trace" => $e->getTraceAsString()]); wp_die(esc_html__("An unexpected error occurred while exporting settings.", "optistate"), esc_html__("Export Error", "optistate"), ["response" => 500]); } } public function ajax_save_max_backups(): void { check_ajax_referer(OPTISTATE::NONCE_ACTION, "nonce"); if (!$this->check_user_access()) { return; } if (!OPTISTATE_Utils::check_rate_limit("save_settings", 3)) { OPTISTATE_Utils::send_json_error(OPTISTATE_Utils::get_rate_limit_message(true), 429); return; } try { $current_settings = $this->get_persistent_settings(); $old_max_backups = (int) $current_settings["max_backups"]; $max_backups_input = isset($_POST["max_backups"]) ? sanitize_text_field(wp_unslash($_POST["max_backups"])) : "3"; if (!is_string($max_backups_input) || !ctype_digit($max_backups_input)) { OPTISTATE_Utils::send_json_error(__("Invalid data format for max backups.", "optistate")); return; } $max_backups = (int) $max_backups_input; if ($max_backups < 1 || $max_backups > 10) { OPTISTATE_Utils::send_json_error(__("Invalid max backups value. Must be between 1 and 10.", "optistate")); return; } $this->save_persistent_settings(["max_backups" => $max_backups]); if ($old_max_backups !== $max_backups) { $this->main_plugin->log_entry("⚙️ " . __("Maximum Backups to Keep Updated by {username}", "optistate")); } OPTISTATE_Utils::send_json_success(["message" => __("Maximum backups setting saved successfully!", "optistate")]); } catch (\Throwable $e) { OPTISTATE_Utils::log_critical_error("ajax_save_max_backups failed: " . $e->getMessage(), ["trace" => $e->getTraceAsString()]); OPTISTATE_Utils::send_json_error(__("An unexpected error occurred while saving the setting.", "optistate")); } } public function ajax_save_auto_settings(): void { check_ajax_referer(OPTISTATE::NONCE_ACTION, "nonce"); if (!$this->check_user_access()) { return; } if (!OPTISTATE_Utils::check_rate_limit("save_settings", 3)) { OPTISTATE_Utils::send_json_error(OPTISTATE_Utils::get_rate_limit_message(true), 429); return; } try { $current_settings = $this->get_persistent_settings(); $old_days = (int) $current_settings["auto_optimize_days"]; $old_time = $current_settings["auto_optimize_time"]; $old_email = (bool) $current_settings["email_notifications"]; $old_max_backups = (int) $current_settings["max_backups"]; $old_auto_backup_only = (bool) $current_settings["auto_backup_only"]; $days_input = isset($_POST["auto_optimize_days"]) ? sanitize_text_field(wp_unslash($_POST["auto_optimize_days"])) : "0"; if (!is_string($days_input) || !ctype_digit($days_input)) { OPTISTATE_Utils::send_json_error(__("Invalid data format for days.", "optistate")); return; } $days = (int) $days_input; if ($days < 0 || $days > 365) { OPTISTATE_Utils::send_json_error(__("Invalid days value. Must be between 0 and 365.", "optistate")); return; } $time_input = isset($_POST["auto_optimize_time"]) ? sanitize_text_field(wp_unslash($_POST["auto_optimize_time"])) : "02:00"; if (!preg_match('/^(0[0-9]|1[0-9]|2[0-3]):00$/', $time_input)) { OPTISTATE_Utils::send_json_error(__("Invalid time value selected.", "optistate")); return; } $email_notifications = isset($_POST["email_notifications"]) && $_POST["email_notifications"] === "1"; $auto_backup_only = isset($_POST["auto_backup_only"]) && $_POST["auto_backup_only"] === "1"; $max_backups_input = isset($_POST["max_backups"]) ? sanitize_text_field(wp_unslash($_POST["max_backups"])) : "3"; if (!is_string($max_backups_input) || !ctype_digit($max_backups_input)) { OPTISTATE_Utils::send_json_error(__("Invalid data format for max backups.", "optistate")); return; } $max_backups = (int) $max_backups_input; if ($max_backups < 1 || $max_backups > 10) { OPTISTATE_Utils::send_json_error(__("Invalid max backups value. Must be between 1 and 10.", "optistate")); return; } $disable_restore_security = isset($_POST["disable_restore_security"]) && $_POST["disable_restore_security"] === "1"; $settings_to_save = ["auto_optimize_days" => $days, "auto_optimize_time" => $time_input, "email_notifications" => $email_notifications, "auto_backup_only" => $auto_backup_only, "max_backups" => $max_backups, "disable_restore_security" => $disable_restore_security,]; $this->save_persistent_settings($settings_to_save); $this->main_plugin->update_cron_schedule($days, $time_input); $settings_changed = $old_days !== $days || $old_time !== $time_input || $old_email !== $email_notifications || $old_auto_backup_only !== $auto_backup_only || $old_max_backups !== $max_backups; if ($settings_changed) { $this->main_plugin->log_entry("⚙️ " . __("Automatic Backup & Cleanup Settings Updated by {username}", "optistate")); } $final_settings = $this->get_persistent_settings(); OPTISTATE_Utils::send_json_success(["message" => __("Settings for scheduled tasks saved successfully!", "optistate"), "days" => $final_settings["auto_optimize_days"], "time" => $final_settings["auto_optimize_time"], "email_notifications" => $final_settings["email_notifications"], "auto_backup_only" => $final_settings["auto_backup_only"], "max_backups" => $final_settings["max_backups"], "disable_restore_security" => $final_settings["disable_restore_security"],]); } catch (\Throwable $e) { OPTISTATE_Utils::log_critical_error("ajax_save_auto_settings failed: " . $e->getMessage(), ["trace" => $e->getTraceAsString()]); OPTISTATE_Utils::send_json_error(__("An unexpected error occurred while saving the settings.", "optistate")); } } public function ajax_export_settings(): void { check_ajax_referer(OPTISTATE::NONCE_ACTION, "nonce"); if (!$this->check_user_access()) { return; } if (!OPTISTATE_Utils::check_rate_limit("impexp_settings", 5)) { OPTISTATE_Utils::send_json_error(OPTISTATE_Utils::get_rate_limit_message(false), 429); return; } try { $download_url = add_query_arg(["action" => "optistate_download_settings", "_wpnonce" => wp_create_nonce("optistate_download_settings"),], admin_url()); OPTISTATE_Utils::send_json_success(["download_url" => esc_url_raw($download_url), "message" => __("Settings export ready.", "optistate"),]); } catch (\Throwable $e) { OPTISTATE_Utils::log_critical_error("ajax_export_settings failed: " . $e->getMessage(), ["trace" => $e->getTraceAsString()]); OPTISTATE_Utils::send_json_error(__("An unexpected error occurred while preparing the export.", "optistate")); } } public function ajax_import_settings(): void { check_ajax_referer(OPTISTATE::NONCE_ACTION, "nonce"); if (!$this->check_user_access()) { return; } if (!OPTISTATE_Utils::check_rate_limit("impexp_settings", 5)) { OPTISTATE_Utils::send_json_error(OPTISTATE_Utils::get_rate_limit_message(false), 429); return; } try { if (!isset($_FILES["settings_file"]) || !is_uploaded_file($_FILES["settings_file"]["tmp_name"])) { OPTISTATE_Utils::send_json_error(__("No file uploaded.", "optistate")); return; } $file = $_FILES["settings_file"]; if ($file["error"] !== UPLOAD_ERR_OK) { OPTISTATE_Utils::send_json_error(__("File upload error.", "optistate")); return; } if ($file["size"] > 1048576) { OPTISTATE_Utils::send_json_error(__("File is too large. Maximum size is 1MB.", "optistate")); return; } $filename = sanitize_file_name($file["name"]); $file_ext = strtolower(pathinfo($filename, PATHINFO_EXTENSION)); if ($file_ext !== "json") { OPTISTATE_Utils::send_json_error(__("Invalid file type. Only JSON files are allowed.", "optistate")); return; } $finfo = null; try { $finfo = finfo_open(FILEINFO_MIME_TYPE); if ($finfo) { $mime_type = finfo_file($finfo, $file["tmp_name"]); $allowed_mimes = ["application/json", "text/plain", "application/octet-stream"]; if (!in_array($mime_type, $allowed_mimes, true)) { OPTISTATE_Utils::send_json_error(__("Invalid file format detected.", "optistate")); return; } } } finally { if ($finfo !== null) { finfo_close($finfo); } } $json_content = file_get_contents($file["tmp_name"]); if ($json_content === false) { OPTISTATE_Utils::send_json_error(__("Failed to read uploaded file.", "optistate")); return; } $import_data = json_decode($json_content, true); if (json_last_error() !== JSON_ERROR_NONE) { OPTISTATE_Utils::send_json_error(__("Invalid JSON file. Error: ", "optistate") . json_last_error_msg()); return; } if (!is_array($import_data) || !isset($import_data["plugin"]) || !isset($import_data["settings"])) { OPTISTATE_Utils::send_json_error(__("Invalid settings file structure.", "optistate")); return; } if ($import_data["plugin"] !== "WP Optimal State") { OPTISTATE_Utils::send_json_error(__("This file is not a valid WP Optimal State settings export.", "optistate")); return; } $new_settings = $import_data["settings"]; if (!is_array($new_settings)) { OPTISTATE_Utils::send_json_error(__("Settings data is corrupted.", "optistate")); return; } $validated_settings = $this->_validate_settings_array($new_settings); if ($validated_settings === false) { OPTISTATE_Utils::send_json_error(__("Settings validation failed. Import aborted.", "optistate")); return; } if (!empty($validated_settings["ip_block_list"])) { $current_user_ip = OPTISTATE_Utils::get_client_ip(!empty($validated_settings["cloudflare_enabled"]), $validated_settings["custom_trusted_proxies"] ?? []); $is_self_blocked = false; foreach ($validated_settings["ip_block_list"] as $rule) { if (OPTISTATE_Utils::ip_in_range($current_user_ip, $rule)) { $is_self_blocked = true; break; } } if ($is_self_blocked) { OPTISTATE_Utils::send_json_error(sprintf(__("Import aborted: Your current IP (%s) is in the blocked list. Please remove it from the imported list and try again.", "optistate"), esc_html($current_user_ip)), 400); return; } } $success = $this->save_persistent_settings($validated_settings); if (!$success) { OPTISTATE_Utils::send_json_error(__("Failed to save imported settings.", "optistate")); return; } $validated_cron_state = null; if (isset($import_data["cron_manager_state"]) && is_array($import_data["cron_manager_state"])) { $validated_cron_state = $this->validate_cron_manager_state($import_data["cron_manager_state"]); $this->main_plugin->set_store_data("cron_manager_state", $validated_cron_state); delete_transient("optistate_cron_jobs_cache"); } if (!empty($validated_settings["login_protect_enabled"]) || !empty($validated_settings["ip_blocker_enabled"])) { $this->main_plugin->login_protection->create_table(); global $wpdb; $table_name = $wpdb->prefix . OPTISTATE_Login_Protection::TABLE_NAME; if (OPTISTATE_Utils::table_exists($table_name)) { $ips = $wpdb->get_col("SELECT ip_address FROM $table_name"); if (!empty($ips)) { $wpdb->query($wpdb->prepare("DELETE FROM {$wpdb->options} WHERE option_name LIKE %s", '_transient_optistate_block_%')); $wpdb->query($wpdb->prepare("DELETE FROM {$wpdb->options} WHERE option_name LIKE %s", '_transient_optistate_clean_%')); } } } if (isset($validated_settings["ip_block_list"])) { $this->main_plugin->login_protection->restore_ip_block_list($validated_settings["ip_block_list"]); } $this->main_plugin->performance_manager->_performance_rebuild_htaccess(); $this->main_plugin->reschedule_cron_from_settings(); $this->main_plugin->invalidate_plugin_caches(); $this->main_plugin->log_entry("📤 " . __("Settings Imported by {username}", "optistate")); if (!class_exists("OPTISTATE_Presets")) { require_once OPTISTATE_INCLUDES_DIR . "class-optistate-presets.php"; } $preset_key = $validated_settings["last_applied_preset"] ?? ""; $preset_label = ""; if (!empty($preset_key)) { $presets = OPTISTATE_Presets::get_presets(); $preset_label = isset($presets[$preset_key]) ? $presets[$preset_key]["label"] : $preset_key; } else { $preset_label = __("None (Default)", "optistate"); } $cron_state_imported = $validated_cron_state !== null; $cron_state_count = $cron_state_imported ? count($validated_cron_state) : 0; $summary = ["max_backups" => $validated_settings["max_backups"], "auto_optimize_days" => $validated_settings["auto_optimize_days"], "email_notifications" => $validated_settings["email_notifications"], "performance_features_count" => count($validated_settings["performance_features"]), "imported_from_site" => isset($import_data["site_url"]) ? esc_url($import_data["site_url"]) : __("Unknown", "optistate"), "exported_at" => isset($import_data["exported_at"]) ? sanitize_text_field($import_data["exported_at"]) : __("Unknown", "optistate"), "preset_label" => $preset_label, "cron_state_imported" => $cron_state_imported, "cron_state_count" => $cron_state_count,]; OPTISTATE_Utils::send_json_success(["message" => __("Settings imported successfully!", "optistate"), "summary" => $summary,]); } catch (\Throwable $e) { OPTISTATE_Utils::log_critical_error("ajax_import_settings failed: " . $e->getMessage(), ["trace" => $e->getTraceAsString()]); OPTISTATE_Utils::send_json_error(__("An unexpected error occurred while importing settings.", "optistate")); } } public function ajax_save_user_access(): void { check_ajax_referer(OPTISTATE::NONCE_ACTION, "nonce"); if (!$this->check_user_access()) { return; } if (!OPTISTATE_Utils::check_rate_limit("save_settings", 3)) { OPTISTATE_Utils::send_json_error(OPTISTATE_Utils::get_rate_limit_message(true), 429); return; } try { $raw_users = isset($_POST["allowed_users"]) && is_array($_POST["allowed_users"]) ? $_POST["allowed_users"] : []; $user_ids = array_filter(array_map("absint", $raw_users), function ($id) { return $id > 0; }); if (empty($user_ids)) { $allowed_users = []; } else { $users = get_users(['include' => $user_ids, 'fields' => ['ID'], 'role__in' => ['administrator'],]); $valid_ids = array_map('intval', wp_list_pluck($users, 'ID')); $allowed_users = array_values(array_unique($valid_ids)); } $current_user_id = get_current_user_id(); if (!empty($allowed_users) && !in_array($current_user_id, $allowed_users, true)) { OPTISTATE_Utils::send_json_error(__("⚠️ Warning: You are about to lock yourself out! Your account is not in the allowed users list. Please include yourself before saving.", "optistate"), 400, ["lock_out_warning" => true]); return; } $old_settings = $this->get_persistent_settings(); $old_allowed_users = $old_settings["allowed_users"] ?? []; $success = $this->save_persistent_settings(["allowed_users" => $allowed_users]); if ($success) { $added_users = array_diff($allowed_users, $old_allowed_users); $removed_users = array_diff($old_allowed_users, $allowed_users); $audit_message = ""; if (!empty($added_users)) { $audit_message .= sprintf("👤 User Access Updated by {username} | Added: %s user(s)", number_format_i18n(count($added_users))); } if (!empty($removed_users)) { $audit_message .= sprintf("👤 User Access Updated by {username} | Removed: %s user(s)", number_format_i18n(count($removed_users))); } if (!empty($audit_message)) { $this->main_plugin->log_entry($audit_message); } $count = count($allowed_users); $message = $count === 0 ? __("All administrators can now access the plugin.", "optistate") : sprintf(_n("Access restricted to %s administrator.", "Access restricted to %s administrators.", $count, "optistate"), number_format_i18n($count)); OPTISTATE_Utils::send_json_success(["message" => $message, "allowed_count" => $count, "audit_logged" => true,]); } else { OPTISTATE_Utils::send_json_error(__("Failed to save user access settings.", "optistate")); } } catch (\Throwable $e) { OPTISTATE_Utils::log_critical_error("ajax_save_user_access failed: " . $e->getMessage(), ["trace" => $e->getTraceAsString()]); OPTISTATE_Utils::send_json_error(__("An unexpected error occurred while saving user access.", "optistate")); } } public function secure_file_write_atomic(string $filepath, string $content, bool $require_csrf = true): bool { $is_system_context = wp_doing_cron() || (defined("WP_CLI") && WP_CLI); if (!$is_system_context && !current_user_can("manage_options")) { return false; } if ($require_csrf && wp_doing_ajax()) { $nonce = $_REQUEST["nonce"] ?? ""; if (!wp_verify_nonce($nonce, OPTISTATE::NONCE_ACTION)) { return false; } } if (!function_exists('get_home_path')) { require_once ABSPATH . 'wp-admin/includes/file.php'; } $wordpress_root = rtrim(get_home_path(), "/\\"); $normalized_path = str_replace("\\", "/", $filepath); $normalized_root = str_replace("\\", "/", $wordpress_root); if (basename($normalized_path) !== ".htaccess" || dirname($normalized_path) !== $normalized_root) { return false; } $fs = $this->main_plugin->get_filesystem(); if (!$fs || !$fs instanceof WP_Filesystem_Base) { return false; } try { $temp_file = $filepath . '.tmp.' . bin2hex(random_bytes(8)); if ($fs->put_contents($temp_file, $content, FS_CHMOD_FILE) === false) { OPTISTATE_Utils::log_critical_error("Atomic write: put_contents failed", ["temp_file" => $temp_file, "filepath" => $filepath, "filesystem_method" => $fs->method]); return false; } $fs->chmod($temp_file, 0644); $backup = $filepath . ".bak"; if ($fs->exists($filepath)) { if (!$fs->copy($filepath, $backup, true)) { OPTISTATE_Utils::log_critical_error("Atomic write: backup copy failed", ["filepath" => $filepath, "backup" => $backup]); $fs->delete($temp_file); return false; } } if (!$fs->move($temp_file, $filepath, true)) { OPTISTATE_Utils::log_critical_error("Atomic write: rename failed", ["temp_file" => $temp_file, "target" => $filepath]); if ($fs->exists($backup)) { $fs->move($backup, $filepath, true); } if ($fs->exists($temp_file)) { $fs->delete($temp_file); } return false; } if ($fs->exists($backup)) { $fs->delete($backup); } return true; } catch (\Throwable $e) { OPTISTATE_Utils::log_critical_error("secure_file_write_atomic exception: " . $e->getMessage(), ["trace" => $e->getTraceAsString()]); return false; } } private function validate_cron_manager_state(array $state): array { $validated = []; foreach ($state as $entry) { if (!is_array($entry)) { continue; } if (!isset($entry["hook"]) || !is_string($entry["hook"])) { continue; } if (!isset($entry["paused"]) && !isset($entry["slowed"])) { continue; } $hook = sanitize_text_field($entry["hook"]); $args = isset($entry["args"]) && is_array($entry["args"]) ? $entry["args"] : []; $valid = ["hook" => $hook, "args" => $args,]; if (isset($entry["original_schedule"]) && is_string($entry["original_schedule"])) { $valid["original_schedule"] = sanitize_text_field($entry["original_schedule"]); } if (isset($entry["original_interval"]) && is_numeric($entry["original_interval"])) { $valid["original_interval"] = (int) $entry["original_interval"]; } if (isset($entry["original_next_run"]) && is_numeric($entry["original_next_run"])) { $valid["original_next_run"] = (int) $entry["original_next_run"]; } if (isset($entry["paused"])) { $valid["paused"] = (bool) $entry["paused"]; } if (isset($entry["slowed"])) { $valid["slowed"] = (bool) $entry["slowed"]; } if (isset($entry["slowed_schedule"]) && is_string($entry["slowed_schedule"])) { $valid["slowed_schedule"] = sanitize_key($entry["slowed_schedule"]); } if (isset($entry["slowed_interval"]) && is_numeric($entry["slowed_interval"])) { $valid["slowed_interval"] = (int) $entry["slowed_interval"]; } if (!empty($valid["slowed"]) && (!isset($valid["slowed_schedule"]) || !isset($valid["slowed_interval"]))) { continue; } $id = md5($hook . serialize($args)); $validated[$id] = $valid; } return $validated; } private function validate_imported_settings(array $settings) { return $this->_validate_settings_array($settings); } public function ajax_save_one_click_extra_items(): void { check_ajax_referer(OPTISTATE::NONCE_ACTION, "nonce"); if (!$this->check_user_access()) { return; } if (!OPTISTATE_Utils::check_rate_limit("save_settings", 3)) { OPTISTATE_Utils::send_json_error(OPTISTATE_Utils::get_rate_limit_message(true), 429); return; } try { $items = isset($_POST["items"]) && is_array($_POST["items"]) ? $_POST["items"] : []; $items = array_map("sanitize_key", $items); $one_click_backup = isset($_POST["one_click_backup"]) && $_POST["one_click_backup"] === "1"; $current_settings = $this->get_persistent_settings(); $old_extra = $current_settings["one_click_extra_items"] ?? []; $new_extra = $items; sort($old_extra); sort($new_extra); $extra_changed = $old_extra !== $new_extra; $old_backup = (bool) ($current_settings["one_click_backup"] ?? false); $backup_changed = $old_backup !== $one_click_backup; $current_settings["one_click_extra_items"] = $items; $current_settings["one_click_backup"] = $one_click_backup; $success = $this->save_persistent_settings($current_settings); if ($success) { if ($extra_changed || $backup_changed) { $this->main_plugin->log_entry("💥 " . __("One-Click configuration updated by {username}", "optistate")); } OPTISTATE_Utils::send_json_success(["message" => __("One-Click configuration saved successfully.", "optistate")]); } else { OPTISTATE_Utils::send_json_error(__("Failed to save One-Click configuration.", "optistate")); } } catch (\Throwable $e) { OPTISTATE_Utils::log_critical_error("ajax_save_one_click_extra_items failed: " . $e->getMessage(), ["trace" => $e->getTraceAsString()]); OPTISTATE_Utils::send_json_error(__("An unexpected error occurred while saving the configuration.", "optistate")); } } }